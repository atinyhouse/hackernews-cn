# 🚀 React版本部署指南

这是使用 Semi Design 组件库重构后的 React 版本部署指南。

## 📋 更新内容

### v3.0.0 - React + Semi Design 重构

- ✅ 使用 React 18 + Vite 重构整个前端
- ✅ 集成 Semi Design 组件库，界面更加现代化
- ✅ 使用 React Router 实现单页应用路由
- ✅ 组件化架构，代码更易维护
- ✅ 保留所有原有功能（搜索、排序、评论树等）

## 🎯 部署步骤

### 1. 确认仓库结构

确保你的仓库包含以下目录结构：

```
hackernews-cn/
├── .github/
│   └── workflows/
│       └── fetch-data.yml      # 已更新为构建React应用
├── backend/                     # 数据抓取脚本（保持不变）
├── frontend/                    # 旧版静态HTML（可选保留）
└── frontend-react/              # 新版React应用
    ├── src/
    ├── public/
    ├── package.json
    └── vite.config.js
```

### 2. GitHub Actions 配置

GitHub Actions workflow 已自动更新，会：
1. 抓取 HackerNews 数据
2. 使用 DeepSeek API 翻译
3. 构建 React 应用
4. 部署到 GitHub Pages

### 3. 手动触发部署

1. 进入仓库的 **Actions** 标签页
2. 选择 "Fetch HackerNews Data" workflow
3. 点击 **"Run workflow"** 按钮
4. 选择 `main` 分支
5. 点击绿色的 **"Run workflow"** 按钮
6. 等待 workflow 完成（约 3-5 分钟）

### 4. 验证部署

部署完成后，访问：
```
https://你的用户名.github.io/hackernews-cn/
```

应该能看到使用 Semi Design 设计的新界面。

## 🛠️ 本地开发

### 安装依赖

```bash
cd frontend-react
npm install
```

### 启动开发服务器

```bash
npm run dev
```

访问 `http://localhost:5173/hackernews-cn/`

### 本地构建测试

```bash
npm run build
```

构建后的文件在 `dist` 目录。

## 🎨 Semi Design 特性

新版本使用了以下 Semi Design 组件：

- **Layout**: 页面布局
- **Card**: 帖子卡片
- **Button**: 按钮
- **Input**: 搜索框
- **Tag**: 标签
- **Typography**: 文字排版
- **Space**: 间距
- **Spin**: 加载动画
- **Empty**: 空状态
- **Descriptions**: 描述列表

## 🔧 配置说明

### vite.config.js

```javascript
export default defineConfig({
  plugins: [react()],
  base: '/hackernews-cn/', // GitHub Pages base path
  build: {
    outDir: 'dist',
  },
})
```

⚠️ **重要**: `base` 必须设置为你的仓库名，否则部署后资源路径会出错。

### 路由配置

使用 React Router 的 `BrowserRouter`，支持：
- `/` - 首页（帖子列表）
- `/post/:id` - 帖子详情页

## 📝 常见问题

### Q: 为什么选择 Semi Design？

A: Semi Design 是字节跳动开源的企业级 UI 组件库，具有：
- 设计现代、美观
- 组件丰富、功能完善
- 中文文档详细
- 性能优秀
- 持续维护

### Q: 旧版静态 HTML 还能用吗？

A: 可以保留 `frontend` 目录作为备份。GitHub Actions 现在只部署 React 版本。

### Q: 如何切回旧版？

A: 修改 `.github/workflows/fetch-data.yml`，将部署目录改回 `./frontend`。

### Q: 构建失败怎么办？

A: 检查以下几点：
1. `package.json` 中的依赖是否正确
2. `vite.config.js` 中的 base 路径是否正确
3. 查看 GitHub Actions 的详细日志

## 🎉 升级完成

恭喜！你的 HackerNews 中文热议榜已经升级到 React + Semi Design 版本！

如有问题，请查看项目 Issues 或提交新的 Issue。
